// app.js

function resolveRequest(id) {
    alert(`Request ${id} marked as resolved!`);
    // TODO: Call backend API to update request status
  }
  
  function blockUser(id) {
    alert(`User ${id} has been blocked!`);
    // TODO: Call backend API to block user
  }
  
  function activateUser(id) {
    alert(`User ${id} has been activated!`);
    // TODO: Call backend API to activate user
  }
  